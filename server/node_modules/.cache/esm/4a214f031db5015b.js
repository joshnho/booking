let express,readdirSync,cors,mongoose;_1db‍.w("express",[["default",["express"],function(v){express=v}]]);_1db‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_1db‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_1db‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);




require('dotenv').config();
const morgan = require('morgan');

const app = express();

// DB connection
const connectDB = () => {
  try {
    mongoose.connect(process.env.DATABASE, {
      useNewUrlParser: true,
      useFindAndModify: false,
      useUnifiedTopology: true,
      useCreateIndex: true,
    });
    console.log('DB connected');
  } catch (error) {
    _1db‍.g.console.log('DB Error =>', error);
  }
};
connectDB();

// Middleware
app.use(cors());
app.use(morgan('dev'));

// Route middleware
readdirSync('./routes').map((r) => app.use('/api', require(`./routes/${r}`)));

const port = process.env.PORT || 8000;

app.listen(port, () => console.log(`Server running on port: ${port}`));
